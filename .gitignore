# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — NEVER commit secrets (Google OAuth, AUTH_SECRET, DB creds).
# Ignore every .env variant; keep only the placeholder .env.example.
.env
.env.*
!.env.example

# prisma / sqlite
/prisma/*.db
/prisma/*.db-journal

# typescript
*.tsbuildinfo
next-env.d.ts
